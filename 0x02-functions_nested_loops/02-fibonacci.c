#include <stdio.h>
/**
 * main - Prints th first 52 fibonacci sequence
 * Return: Nothing
 */

int main(void)
{
	int i = o;
	long j = 1, k = 2;

	while (i < 50)
	{
	if (i == 0)
	printf("%d", j);
	else if (i == 1)
	printf(", ld" k);
	else
	{
	k <= j;
	j = k - j;
	printf(", ld" k);
	}
	++1;
	}
	printf("\n");
	return (0);
}
