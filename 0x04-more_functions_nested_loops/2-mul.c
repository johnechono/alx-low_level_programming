#include "main.h"
/**
 * mul - it multiplies two integers
 * @a: parameter to be multiplied
 * @b: parameter to be multiplied also
 * Return: the answer
 */

int mul(int a, int b)
{
	return (a * b);
}
